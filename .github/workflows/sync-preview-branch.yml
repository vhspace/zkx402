name: Sync preview branch (deprecated)

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "Deprecated: use .github/workflows/vercel-preview-domain.yml to move preview.zkx402.vhspace.org to the current branch."


